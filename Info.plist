<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- ═══ Native iPad Resolution (CRITICAL - forces full-screen, not iPhone-compat zoom) ═══ -->
    <key>UILaunchScreen</key>
    <dict/>

    <!-- ═══ Kiosk / Immersion ═══ -->
    <key>UIRequiresFullScreen</key>
    <true/>
    <key>UISupportsFullScreenInEveryConfiguration</key>
    <true/>
    <key>UIStatusBarHidden</key>
    <true/>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>

    <!-- ═══ Networking — Local + HTTP ═══ -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>
    <key>NSLocalNetworkUsageDescription</key>
    <string>Velora needs local network access to connect to your Navidrome server and stream your music library.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_subsonic._tcp</string>
        <string>_http._tcp</string>
    </array>

    <!-- ═══ Scene Configuration ═══ -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <true/>
        <key>UISceneConfigurations</key>
        <dict/>
    </dict>
</dict>
</plist>
