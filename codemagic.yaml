workflows:
  ios-jailbreak:
    name: Velora Jailbreak IPA (Unsigned)
    instance_type: mac_mini_m1
    max_build_duration: 60
    cache:
      cache_paths:
        - ~/.swiftpm
    environment:
      xcode: "16"
    scripts:
      - name: Build
        script: |
          xcrun swift package resolve || true
          xcrun swift package generate-xcodeproj --scheme Velora || true
          xcodebuild -project Velora.xcodeproj \
            -scheme Velora \
            -configuration Debug \
            -destination generic/platform=iOS \
            -archivePath build/Velora.xcarchive \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            SKIP_INSTALL=YES
          mkdir -p Payload
          find build/Velora.xcarchive -name "*.app" -type d -exec cp -R {} Payload/Velora.app \;
          zip -r build/Velora-Jailbreak.ipa Payload
          mkdir -p build/ios
          mv build/Velora-Jailbreak.ipa build/ios/

    artifacts:
      - build/ios/*.ipa