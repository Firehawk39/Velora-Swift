fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
httpx>=0.27.0
# sqlite-vec is typically loaded as an extension to standard sqlite3
# or via the `sqlite-vec` python wrapper if available. We will rely on standard sqlite3 for Phase 1.
